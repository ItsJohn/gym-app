{
  "test": {
    "framework": "jest",
    "setupFiles": ["jest.setup.js"],
    "mocking": [
      "expo-sqlite should always be mocked in tests, including openDatabaseAsync and deleteDatabaseAsync.",
      "Reset all mockResolvedValue and mockRejectedValue in beforeEach to avoid test bleedover.",
      "Use resetDatabase() from database module to clear internal state before each test."
    ],
    "run": "nvm use && yarn test"
  },
  "codeStyle": {
    "typescript": {
      "quotes": "double",
      "semi": true,
      "trailingComma": "all",
      "tabWidth": 2
    },
    "prettier": true,
    "eslint": true
  },
  "ai": {
    "no_comments_in_tests": true,
    "prefer_comprehensive_tests": true,
    "always_reset_mocks": true,
    "always_reset_db_state": true
  }
}
